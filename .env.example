# Copiá este archivo a .env (git lo ignora) y completá con tus valores:
#   cp .env.example .env

DB_PASSWORD=lasmelis_dev
JWT_KEY=cambiame-por-una-clave-secreta-larga-de-al-menos-32-caracteres

# Solo para docker-compose.registry.yml (imágenes ya publicadas en un registry,
# en vez de buildearlas local con docker-compose.yml):
BACKEND_IMAGE=ghcr.io/julietamelinsky01/lasmelis-backend:v0.1.0
FRONTEND_IMAGE=ghcr.io/julietamelinsky01/lasmelis-frontend:v0.1.0
