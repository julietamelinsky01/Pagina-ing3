server {
    listen 80;

    # SPA con react-router (BrowserRouter): fallback a index.html para que
    # rutas como /empleados o /calendario no den 404 al refrescar.
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
